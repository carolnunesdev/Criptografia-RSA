# 🔐 CRIPTOGRAFIA RSA: ANÁLISE TÉCNICA E REQUISITOS DE IMPLEMENTAÇÃO

> Este documento detalha os princípios, o funcionamento, a segurança e as limitações do algoritmo RSA (Rivest-Shamir-Adleman), ideal para documentação técnica e estudos aprofundados.

---

## 📊 Índice Detalhado

1.  [Introdução e Características do RSA](#1-introdução-e-características-do-rsa)
2.  [Fundamentos Matemáticos Sólidos](#2-fundamentos-matemáticos-sólidos)
3.  [Algoritmo RSA Passo-a-Passo](#3-algoritmo-rsa-passo-a-passo)
4.  [💻 Implementação Detalhada e Algoritmos Essenciais](#4-implementação-detalhada-e-algoritmos-essenciais)
5.  [🔒 Análise de Segurança e Tamanhos de Chave](#5-análise-de-segurança-e-tamanhos-de-chave)
6.  [⚡ Otimizações e Considerações de Performance](#6-otimizações-e-considerações-de-performance)
7.  [🚨 Limitações, Ataques e Contramedidas](#7-limitações-ataques-e-contramedidas)
8.  [🔄 Conclusões, Comparação e Uso Recomendado](#8-conclusões-comparação-e-uso-recomendado)

---

## 1. Introdução e Características do RSA

O **RSA** (Rivest-Shamir-Adleman), desenvolvido em 1977, é um algoritmo de **criptografia assimétrica** [1] e um dos mais amplamente utilizados sistemas de chave pública [1].

### 1.1 Características Principais

*   **Assimétrico:** Usa duas chaves diferentes: **pública** e **privada** [2].
*   **Baseado em problema matemático:** A segurança reside na **dificuldade de fatorar números grandes** [2].
*   **Bidirecional:** Pode criptografar e realizar **assinatura digitalmente** [2, 3].
*   **Maturidade:** Possui **45+ anos de análise criptográfica** [3].
*   **Ampla Adoção:** É a **base de muitos protocolos** de segurança, como HTTPS e SSH [2, 3].

### 1.2 Princípio Básico

O fluxo de comunicação utiliza a separação das chaves [2]:
1.  Bob usa a **chave pública de Alice** para criptografar.
2.  Apenas Alice pode descriptografar (usando sua **chave privada secreta**) [2].

## 2. Fundamentos Matemáticos Sólidos

O RSA é fundamentado em problemas bem estudados da Teoria dos Números [3].

### 2.1 Aritmética Modular

É **fundamental para o RSA** [4]. A operação $a \equiv b \pmod n$ significa que $a \mod n = b \mod n$ [4]. A exponenciação modular, $(a^k) \mod n$, deve ser calculada de forma **eficiente** [4].

### 2.2 Números Primos e Fatoração

*   **Teorema Fundamental da Aritmética:** Todo número tem fatoração prima única [4].
*   **Problema da Fatoração:** Dado o módulo $n = p \times q$, encontrar os primos $p$ e $q$ é **computacionalmente difícil para números grandes**, o que garante a segurança [4, 5].

### 2.3 Função Totiente de Euler ($\varphi(n)$)

A função $\varphi(n)$ representa a quantidade de números menores que $n$ que são **coprimos** com $n$ [6].

*   Para o produto de primos $n = p \times q$: $$\varphi(n) = (p-1) \times (q-1) \text{ [6, 7]}$$
*   Este valor **deve ser mantido em segredo** [7].

### 2.4 Teorema de Euler e Inverso Modular

*   **Teorema de Euler:** Se $\text{gcd}(a, n) = 1$, então $a^{\varphi(n)} \equiv 1 \pmod n$ [6].
*   **Corolário:** $a^{(k\times\varphi(n) + 1)} \equiv a \pmod n$ [6]. Este corolário é a prova matemática de que a descriptografia funciona, pois $m^{ed} \equiv m \pmod n$ [8].
*   **Inverso Modular ($d$):** O expoente privado $d$ é o inverso modular de $e$ módulo $\varphi(n)$ [6, 9]. É calculado usando o **Algoritmo Euclidiano Estendido** [6, 9].

## 3. Algoritmo RSA Passo-a-Passo

### Fase 1: Geração de Chaves

1.  **Gerar Primos ($p, q$):** Gere dois números primos **distintos**, com tamanhos similares ($\approx n/2$ bits cada) [7].
2.  **Calcular Módulo ($n$):** $n = p \times q$. O módulo $n$ é **público** [7].
3.  **Calcular $\varphi(n)$:** $\varphi(n) = (p-1) \times (q-1)$ [9].
4.  **Escolher Expoente Público ($e$):** Escolha $e$ tal que $1 < e < \varphi(n)$ [9]. O valor **$e = 65537$** é comum por ser primo, ter poucos bits "1" (tornando a exponenciação eficiente) e ser amplamente testado [9].
5.  **Calcular Expoente Privado ($d$):** $d = e^{-1} \mod \varphi(n)$ [9].

**Resultado:** **Chave Pública** $(n, e)$ e **Chave Privada** $(n, d)$ [9].

### Fase 2: Criptografia

Para criptografar a mensagem $m$ (onde $m$ é um número e $m < n$) usando a chave pública $(n, e)$:
$$c = m^e \mod n \text{ [8]}$$

### Fase 3: Descriptografia

Para descriptografar o texto cifrado $c$ usando a chave privada $(n, d)$:
$$m = c^d \mod n \text{ [8]}$$

## 4. 💻 Implementação Detalhada e Algoritmos Essenciais

### 4.1 Teste de Primalidade Miller-Rabin

É um algoritmo probabilístico essencial para gerar $p$ e $q$ [10].

*   **Complexidade:** $O(k \times \log³ n)$ [11].
*   **Precisão:** A **probabilidade de erro é menor que $(1/4)^k$**, onde $k$ é o número de *rounds* de teste [11].

### 4.2 Exponenciação Modular Rápida ("Square-and-Multiply")

*   É o algoritmo central para calcular $a^b \mod n$ [11].
*   **Complexidade:** $O(\log \exp)$ [12].
*   **Função:** Evita calcular **números gigantescos**, o que é **fundamental para viabilizar o RSA** [12].

### 4.3 Algoritmo Euclidiano Estendido (AEE)

*   **Função:** Encontra coeficientes $x$ e $y$ tal que $ax + by = \text{gcd}(a, b)$ [12, 13].
*   É usado para encontrar o inverso modular $d$ do expoente público $e$ [13].

### 4.4 Conversão Mensagem ↔ Números

Para fins educacionais, pode-se processar um caractere por vez [13]. **Em produção**, esta etapa exige o uso de **padding (OAEP)** e o processamento de **blocos maiores** que respeitem a limitação $m < n$ [13].

## 5. 🔒 Análise de Segurança e Tamanhos de Chave

A segurança do RSA depende da dificuldade do **Problema da Fatoração** [5].

### 5.1 Algoritmos de Fatoração Clássicos

O melhor algoritmo clássico conhecido é o **General Number Field Sieve (GNFS)** [5].

*   Para um $n$ de 2048 bits, o GNFS exige $\sim 2^{112}$ operações [5].

### 5.2 Tamanhos de Chave Recomendados

| Ano | Tamanho Mínimo (RSA) | Equivalência Simétrica | Status |
| :---: | :---: | :---: | :---: |
| 2010 | 1024 bits | 80 bits | ❌ Quebrado [5] |
| 2015 | 2048 bits | 112 bits | ✅ Seguro atual [5] |
| **2025** | **3072 bits** | **128 bits** | ✅ **Recomendado** [14] |
| 2030 | 4096 bits | 140 bits | 🔮 Futuro [14] |

## 6. ⚡ Otimizações e Considerações de Performance

O RSA é **lento comparado a algoritmos simétricos** [3], exigindo otimizações **cuidadosas** [3].

### 6.1 Otimização: Chinese Remainder Theorem (CRT)

O CRT é a principal otimização para a descriptografia, fornecendo um **Speedup de $\sim 4\text{x}$** [15].

*   Em vez de $m = c^d \mod n$, o cálculo é dividido em dois módulos menores (módulo $p$ e $q$) e depois combinado [15].
*   Requer que a chave privada armazene valores pré-computados, como $p, q, d_p, d_q$ e $q_{\text{inv}}$ [15].

### 6.2 Operações Mais Custosas

1.  Geração de primos: $O(\log⁴ n)$ [16].
2.  Exponenciação modular: $O(\log³ n)$ [16].
3.  Inversão modular: $O(\log² n)$ [16].

## 7. 🚨 Limitações, Ataques e Contramedidas

### 7.1 Limitações Fundamentais

*   **Performance:** RSA é **$\sim 1000\text{x}$ mais lento que AES** [17]. Uso típico é **híbrido** (criptografar chave simétrica, não dados diretamente) [17].
*   **Tamanho da Mensagem:** A mensagem $m$ deve ser menor que $n$ ($\sim 255$ bytes para 2048 bits) [17].

### 7.2 Ataques e Contramedidas

| Tipo de Ataque | Descrição | Contramedida |
| :--- | :--- | :--- |
| **Ataque de Padding** (Bleichenbacher) | Explorar oráculos de *padding* contra PKCS#1 v1.5 [14]. | Usar **Padding OAEP** para criptografia [18, 19]. |
| **Side-Channel** (Timing Attacks) | Análise de tempo de cálculo ou consumo de energia [14]. | Implementar operações em **tempo constante** (`constant_time_exp`) [18, 19]. |
| **Pequeno Expoente Privado** | Vulnerável ao ataque de Wiener [14, 20]. | Garantir que $d$ tenha tamanho adequado ($\ge n/4$ bits) [20]. |
| **Implementação Insegura** | Falha na limpeza de memória, validação não constante de tempo [16, 20]. | Implementar **limpeza de dados sensíveis** (`impl Drop`) e **validação constante de tempo** [16, 20]. |

### 7.3 Vulnerabilidade Quântica

O **Algoritmo de Shor (1994)** quebra o RSA em tempo polinomial [17].

*   Requer computador quântico com $\sim 4096$ qubits lógicos [17].
*   **Necessita migração futura** para criptografia pós-quântica (PQC) [3, 17].

## 8. 🔄 Conclusões, Comparação e Uso Recomendado

### 8.1 Pontos-Chave e Limitações (Resumo)

| Pontos-Chave | Limitações Importantes |
| :--- | :--- |
| **Fundamento Matemático Sólido** [3] | **Performance** (Lento, requer otimizações como CRT) [3] |
| **Versatilidade** (Criptografia e Assinatura Digital) [3] | **Vulnerabilidade Quântica** (Quebrado pelo Algoritmo de Shor) [3] |
| **Maturidade** (45+ anos de análise) [3] | **Complexidade de Implementação** (Fácil de implementar incorretamente) [21] |

### 8.2 RSA vs. Outros Algoritmos

| Aspecto | RSA 2048 | ECC 256 | Kyber (Pós-Quântico) |
| :---: | :---: | :---: | :---: |
| Segurança | ~112 bits [22] | ~128 bits [22] | ~1KB [22] |
| Tamanho chave | 2048 bits [22] | 256 bits [22] | Lattice-based [22] |
| Risco Quântico | ❌ Vulnerável [22] | ❌ Vulnerável [22] | **✅ Resistente** [22] |

### 8.3 Uso Recomendado

| ✅ USAR RSA para: [21] | ❌ NÃO usar RSA para: [21] |
| :--- | :--- |
| Troca de chaves simétricas | Criptografia de dados grandes |
| Assinatura digital (com **PSS**) | Novos sistemas (preferir ECC) |

### 8.4 Implementação Educacional vs. Produção

A implementação educacional foca na clareza e usa chaves pequenas (512 bits) e sem *padding* seguro [21].

Para **produção**, é obrigatório usar:
*   **Bibliotecas testadas** (OpenSSL, BoringSSL) [19].
*   **Chaves $\ge 2048$ bits** [19].
*   **Padding OAEP** para criptografia e **PSS** para assinatura digital [19].
*   **Proteções contra *timing attacks*** [19].

***
🎓 "O RSA nos ensina que a matemática pode ser tanto elegante quanto prática, fornecendo segurança através da beleza dos números primos." [19]
***
